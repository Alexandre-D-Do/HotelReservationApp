<UserControl x:Class="HotelReservationApp.Views.ReservationListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HotelReservationApp.Views" xmlns:converters="clr-namespace:HotelReservationApp.Converters" 
             xmlns:custom="clr-namespace:LoadingSpinnerControl;assembly=LoadingSpinnerControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <custom:LoadingSpinner Diameter="100" Thickness="5" Color="White"  IsLoading="{Binding IsLoading}"/>
        <Grid Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Grid.Column="0" FontSize="24" Text="View Reservations" Margin="0, 10, 0 ,0"/>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Visibility="{Binding HasErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0, 25, 0 ,0"/>
                    <TextBlock Text="No reservations have been made." Visibility="{Binding HasReservations, Converter={StaticResource InverseBooleanToVisibilityConverter}}" Margin="0, 25, 0 ,0" />
                </StackPanel>
                <Grid Visibility="{Binding HasReservations, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ListView Style="{StaticResource DarkListView}" SelectedItem="{Binding SelectedReservation}" Grid.Row="1" Margin="0, 50, 0 ,0" ItemsSource="{Binding Reservations}" 
                          Visibility="{Binding HasErrorMessage, Converter={StaticResource InverseBooleanToVisibilityConverter}}" SelectionChanged="ListView_SelectionChanged">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Room ID">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock HorizontalAlignment="Center" Text="{Binding RoomID}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Username">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock HorizontalAlignment="Center" Text="{Binding Username}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Start Date">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock HorizontalAlignment="Center" Text="{Binding StartDate}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="End Date">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock HorizontalAlignment="Center" Text="{Binding EndDate}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Grid>
            <Button Style="{StaticResource ApplicationButton}" Grid.Row="2" Margin="0, 5 ,0, 0" Content="Delete Reservation" Command="{Binding DeleteReservationCommand}" FontSize="10" Height="25" Width="100" HorizontalAlignment="Left" Visibility="{Binding HasReservations, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <Button Style="{StaticResource ApplicationButton}" Grid.Row="2" Margin="0, 5 ,0, 0" Content="Make Reservation" Command="{Binding MakeReservationCommand}" FontSize="10" Height="25" Width="100" HorizontalAlignment="Right" Visibility="{Binding HasReservations, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <Button Style="{StaticResource ApplicationButton}" Grid.Row="2" Margin="0, 10 ,0, 0" Content="Make Reservation" Command="{Binding MakeReservationCommand}" FontSize="10" Height="25" Width="100"  HorizontalAlignment="Left" VerticalContentAlignment="Center" Visibility="{Binding HasReservations, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</UserControl>
