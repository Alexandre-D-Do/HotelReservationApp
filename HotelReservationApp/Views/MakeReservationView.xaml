<UserControl x:Class="HotelReservationApp.Views.MakeReservationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HotelReservationApp.Views" xmlns:converters="clr-namespace:HotelReservationApp.Converters" xmlns:custom="clr-namespace:LoadingSpinnerControl;assembly=LoadingSpinnerControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <custom:LoadingSpinner Diameter="100" Thickness="5" Color="White"  IsLoading="{Binding IsLoading}"/>
        <Grid Grid.IsSharedSizeScope="True" Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" FontSize="24" Text="Make Reservation" />

            <Grid Grid.Row="1" Margin="0, 25, 0 ,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Username" />
                <TextBox Grid.Row="1" Width="300" HorizontalAlignment="Left"  Margin="0, 5, 0 ,0" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>

            <Grid Grid.Row="2" Margin="0, 10, 0 ,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" SharedSizeGroup="FirstColumn" />
                    <ColumnDefinition Width="auto" SharedSizeGroup="SecondColumn"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Floor Number" />
                <TextBox Grid.Row="1" Grid.Column="0" Margin="0, 5, 0 ,0" PreviewKeyDown="WhiteSpaceNegation" PreviewTextInput="NumberValidation" Text="{Binding FloorNumber, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="0" Grid.Column="1" Margin ="5, 0, 0, 0" Text="Room Number" />
                <TextBox Grid.Row="1" Grid.Column="1" Margin="10, 5, 0 ,0" PreviewKeyDown="WhiteSpaceNegation" PreviewTextInput="NumberValidation" Text="{Binding RoomNumber, UpdateSourceTrigger=PropertyChanged}"/>

            </Grid>

            <Grid Grid.Row="3" Margin="0, 25, 0 ,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" SharedSizeGroup="FirstColumn" />
                    <ColumnDefinition Width="auto" SharedSizeGroup="SecondColumn"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Start Date" />
                <DatePicker Grid.Row="1" Grid.Column="0" Margin="0, 5, 0 ,0" SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="0" Grid.Column="1" Margin ="5, 0, 0, 0" Text="End Date"/>
                <DatePicker Grid.Row="1" Grid.Column="1" Margin="10, 5, 0 ,0" SelectedDate="{Binding EndDate,UpdateSourceTrigger=PropertyChanged}"/>

            </Grid>

            <StackPanel Grid.Row= "4" Margin="0, 25, 0, 0" Orientation="Horizontal">
                <Button Style="{StaticResource ApplicationButton}" Content="Submit" Width="75" Height="25" FontSize="10" Command="{Binding SubmitCommand}"/>
                <Button Content="Cancel" Width="75" Height="25" FontSize="10" Margin="10,0, 0, 0" Command="{Binding CancelCommand}">
                    <Button.Style>
                        <Style BasedOn="{StaticResource ApplicationButton}" TargetType="Button">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSubmitting}" Value="True">
                                    <Setter Property="IsEnabled" Value="false"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <custom:LoadingSpinner
                    Margin="10 0 0 0"
                    VerticalAlignment="Center"
                    Diameter="15"
                    IsLoading="{Binding IsSubmitting}"
                    Thickness="3"
                    Color="White" />
            </StackPanel>
            <TextBlock
            Grid.Row="5"
            Margin="0 20 0 0"
            Foreground="Red"
            Text="{Binding SubmitErrorMessage}"
            Visibility="{Binding HasSubmitErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </Grid>
    </Grid>
</UserControl>
